{
  "name": "@mithraic-labs/serum-remote",
  "version": "1.0.0",
  "description": "A node process for monitoring and executing permissionless Serum Remote orders",
  "main": "index.ts",
  "scripts": {
    "showConfig": "ts-node --show-config",
    "dev": "ts-node src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mithraic-labs/serum-remote": "^0.0.1-rc.8",
    "@project-serum/anchor": "^0.23.0"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.0",
    "@types/node": "^17.0.12",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5"
  }
}
